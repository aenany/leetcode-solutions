/**
 * @param {number} area
 * @return {number[]}
 */
var constructRectangle = function(area) {
    var rectangle = [];
    var length, width;

    //you have an area;

    //start by drawing a square
    
};